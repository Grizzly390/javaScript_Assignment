//Write a Javascript program to calculate the Body Mass Index (BMI) using the formula BMI = weight (kg)/height * height.


const weight=80;
const heightInCentimeter=173;

const heightiInMeter= heightInCentimeter/100

const bmiResult = (weight)/(heightiInMeter*heightiInMeter)

console.log(bmiResult);
